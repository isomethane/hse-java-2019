language: java
jdk:
  - oraclejdk11
os:
  - linux
script:
  - for PROJECT_DIR in `find * -maxdepth 0 -type d -not -name '.git'`; do
      cd $PROJECT_DIR;
      ./gradlew check;
      cd ..;
    done
